let vel = Number(window.write('Você passou no radar a quantos km/h?'))
if (vel > 50) {
    console.log('Você ultrapassou o limite de velocidade permitido. Multado!')
} else {
    console.log('Você não ultrapassou o limite de velocidade. A multa não será aplicada!')
}